apiVersion: v1
kind: Service
metadata:
  name: titan-service
spec:
  type: LoadBalancer   # ✅ NOW IT WILL CREATE A PUBLIC LB
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: titan-website